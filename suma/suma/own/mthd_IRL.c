#include "../own/header_IRL.h"
#include "../defineHeader.h"
/*
// draw and display
    al_draw_bitmap(bkgImg, 0, 0, 0);
    //MY_ALGO_DrawCandyImgs(mainArray, row, col, candyImg);
    al_flip_display();

    int x = 0,y = 0 ,i ;

    for(i = 0; x > -3200 ; i--)
    {
        al_draw_bitmap(bkgImg, x, y, 0); // Draw bitmap
        al_flip_display(); // Flip display to show the drawing result
        al_rest(0.01); // Delay
        x = x - 1;
        al_clear_to_color(al_map_rgb(0, 0, 0)); // Clear display
    }
*/
